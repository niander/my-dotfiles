# Runs after oh-my-zsh, so a zstyle here replaces omz's rather than adding to it.
# Matching, menu selection and the cache come from omz.

# pasting with tabs doesn't perform completion
zstyle ':completion:*' insert-tab pending
