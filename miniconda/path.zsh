# added by Miniconda2 4.0.5 installer
[[ -d $HOME/miniconda2/bin ]] && export PATH="$HOME/miniconda2/bin:$PATH" && exit

# added by Miniconda3 installer
[[ -d $HOME/miniconda3/bin ]] && export PATH="$HOME/miniconda3/bin:$PATH"
