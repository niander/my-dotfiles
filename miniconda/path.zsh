# added by Miniconda2 4.0.5 installer
[[ -d $HOME/miniconda2/bin ]] && export PATH="$HOME/miniconda2/bin:$PATH" && return

# added by Miniconda3 installer
[[ -d $HOME/miniconda3/bin ]] && source $HOME/miniconda3/etc/profile.d/conda.sh # export PATH="$HOME/miniconda3/bin:$PATH"
