# Extra git aliases. omz's git plugin loads later, so anything it also defines wins.
alias gac='git add --all && git commit -m'
alias gcpb='git copy-branch-name'
alias gcb='git copy-branch-name'
alias ge='git-edit-new'
